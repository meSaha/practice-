# Module | Creating your own module | Date and Math Module
# The Date Module
import datetime

print(datetime.datetime.now())
time_now = datetime.datetime.now()
# time_now.strftime(format) - format can be various types
print(time_now.strftime("%B"))
